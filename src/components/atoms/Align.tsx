import { Box, styled } from '@mui/material';

export const Align = styled(Box)`
  display: grid;
  grid-template-columns: auto 1fr;
  align-items: center;
  column-gap: 2.5rem;
  row-gap: 1rem;
  margin-top: 1rem;
  margin-bottom: 1rem;
`;
